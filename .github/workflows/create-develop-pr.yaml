name: Release

on:
  push:
    branches:
      - develop

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - name: Check if PR exists
        uses: actions/checkout@v3
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [[ $(gh pr list --state open --head feature) == "" ]]; then
            gh pr create --title "Feature 123" --body "This PR adds a new feature."
          fi
