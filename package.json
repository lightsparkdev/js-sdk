{
  "name": "js",
  "description": "Lightspark JS Projects",
  "version": "1.0.0",
  "main": "index.js",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.9.4"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.1"
  }
}
