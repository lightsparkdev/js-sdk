/* This file must be named eslintrc.js to avoid being overwritten by the build step by an
   autogenerated config. The autogenerated config does some things we need to override -
   most settings from the config are replicated here. */
module.exports = {
  extends: [
    "@lightsparkdev/eslint-config/base",
    "universe/native",
    "universe/web",
  ],
  ignorePatterns: ["build"],
  overrides: [
    {
      files: ["**/__tests__/*"],
      env: { node: true },
      globals: { __DEV__: true },
    },
  ],
  rules: {
    /* Import order managed by prettier: */
    "import/order": "off",
    "no-restricted-imports": [
      "warn",
      {
        // fbjs is a Facebook-internal package not intended to be a public API
        patterns: ["fbjs/*", "fbjs"],
      },
    ],
  },
};
