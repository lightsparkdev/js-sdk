import type { FilterBarConfig } from "../src/components/FilterBar";

const legacyCompleteConfig: FilterBarConfig = {
  operator: "is",
  emptyValue: "Empty",
  apply: "Apply",
  addFilter: "Filter",
  clearFilters: "Clear",
};

void legacyCompleteConfig;
