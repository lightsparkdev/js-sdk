{
  "extends": "tsconfig/base.json",
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "moduleResolution": "node" /* Can use node16 or nodenext here to match base? */,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "noEmit": true
  }
}
